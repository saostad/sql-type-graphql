{{#*inline "graphqlTypePartial"}}
@ObjectType()
export class {{name}} {
  {{log columns}}{{#each columns}}

  @Field(()=> {{sqlTypeToGqlType type isPrimaryKey}}, { description: "DB Type {{type}}" {{#if nullable}}, nullable: true{{/if}} })
  {{name}}{{#if nullable}}?{{/if}}: {{#if isPrimaryKey}}string{{else}}{{propertyType}}{{/if}}
  {{/each}}
}
{{/inline}}
import { Field, Int, Float, ObjectType } from "type-graphql";

{{> graphqlTypePartial table}}